# 前端设计说明

## 项目介绍

本项目是展会现场使用的扫码展示系统，用于把展品二维码和大屏展示内容连接起来。

系统分为两端：

```text
大屏端：给观众看，用于待机、欢迎、展示内容
后台端：给工作人员用，用于维护展会项目、编号内容、图片和部署状态
```

核心规则：

```text
一个项目 = 一场展会或一套展示方案
一个项目有一套独立编号
同一个编号在不同项目下可以对应不同展示内容
当前只部署一个项目
扫码后只读取当前部署项目下的编号和页面
```

现场典型流程：

```text
1. 管理员登录后台
2. 新建或选择一个展会项目
3. 配置该项目的待机页、欢迎页、默认图片和主题色
4. 为该项目维护一组编号展示页
5. 点击“部署项目”
6. 大屏打开 /display
7. 观众扫码后，大屏按当前部署项目展示对应编号内容
```

## 信息架构

```text
/login
后台登录页

/admin
项目内容管理页

/display
大屏展示页
```

接口数据来源：

```text
projects
展会项目、待机页、欢迎页、默认图片、主题色、部署状态

pages
项目内编号展示页

scans
扫码记录

admin_users / admin_sessions
管理员账号和登录会话
```

## 页面一：后台登录页

路由：

```text
/login
```

使用对象：

```text
现场管理员、内容维护人员
```

页面目标：

```text
验证管理员身份，进入后台管理页。
```

当前账号：

```text
admin / 123456
```

页面内容：

```text
系统名称：展示后台
英文辅助标题：Expo Display Admin
管理员账号输入框
密码输入框
登录按钮
错误提示区域
```

设计重点：

```text
登录页应简洁、居中、稳定。
不需要营销化视觉。
错误信息要清晰，例如“用户名或密码错误”。
适合现场临时电脑使用，输入框和按钮要足够大。
```

状态：

```text
未输入：等待输入账号和密码
登录中：按钮可显示加载状态
失败：显示错误提示
成功：跳转 /admin
已登录访问 /login：自动跳转 /admin
```

## 页面二：后台项目管理页

路由：

```text
/admin
```

使用对象：

```text
现场管理员、内容维护人员
```

页面目标：

```text
按项目管理整套展会展示内容，并选择当前部署项目。
```

权限：

```text
未登录访问 /admin 会跳转 /login
保存项目、保存编号、上传图片、部署项目都需要登录
扫码上报接口不需要登录
```

### 左侧区域：项目与编号导航

用途：

```text
选择当前编辑的展会项目
新建项目
部署项目
查看当前项目是否已部署
选择或新建该项目下的展项编号
```

内容：

```text
品牌区：展示后台 / Expo Display Admin
展会项目下拉框
新建项目按钮
部署项目按钮
部署状态文字
新建编号按钮
编号列表
```

设计重点：

```text
“已部署”状态必须明显。
项目选择和编号选择要分层清楚，避免误以为编号跨项目共享。
编号列表建议显示：编号 + 页面标题。
当前选中的编号需要有明确高亮。
```

### 主区域顶部：页面标题与操作

内容：

```text
标题：项目内容管理
说明：一个项目一套编号。部署项目后，大屏和扫码都使用该项目内容。
当前账号
打开展示页
退出
```

设计重点：

```text
“打开展示页”是现场预览入口。
“退出”不应抢主视觉。
当前账号只作为状态展示。
```

### 模块一：待机页与欢迎页配置

用途：

```text
配置当前项目在大屏上的项目级内容。
这些内容不属于某个编号，而属于整个展会项目。
```

字段：

```text
项目名称
项目主题色
待机页小标题
待机页主标题
待机页提示
欢迎页小标题
欢迎页主标题
欢迎页副标题
默认图片地址
上传默认图片
```

字段解释：

```text
项目名称：后台识别项目用，例如“2026 科创成果展”
项目主题色：待机页、欢迎页和默认展示视觉使用
待机页小标题：大屏空闲时顶部小字，例如 Achievement Expo
待机页主标题：大屏空闲时最大标题，例如 成果展示互动屏
待机页提示：引导观众扫码，例如 请扫描展品二维码
默认图片地址：没有给编号单独配置图片时使用
上传默认图片：上传后写入默认图片地址
```

模板变量：

```text
{title}    当前编号展示页标题
{code}     当前扫码编号
{project}  当前项目名称
```

示例：

```text
欢迎页主标题：欢迎参观 {title}
欢迎页副标题：当前展项编号 {code}
```

### 模块二：编号展示页配置

用途：

```text
维护当前项目下每个扫码编号对应的最终展示内容。
```

字段：

```text
展项编号
展示页标题
展示页副标题
展示页正文
展示页主题色
上传展示图片
展示图片地址
预览区域
保存编号
模拟扫码
```

字段解释：

```text
展项编号：从二维码 URL 解析出来的编号，例如 10100043
展示页标题：最终展示页最大标题
展示页副标题：标题下的辅助说明
展示页正文：项目介绍、展品亮点、团队说明等
展示页主题色：该编号页面可覆盖项目主题色
展示图片地址：该编号页面的主图；为空时使用项目默认图片
模拟扫码：仅当当前项目已部署时，用当前编号触发大屏展示
```

设计重点：

```text
这里要让用户明确：编号属于当前项目。
切换项目后，编号列表也随之切换。
保存编号和部署项目是两个不同动作。
保存只保存内容；部署决定大屏和扫码使用哪一套内容。
```

## 页面三：大屏展示页

路由：

```text
/display
```

使用对象：

```text
展会观众、现场大屏
```

页面目标：

```text
在无人扫码时展示项目待机页。
扫码后直接进入编号展示页。
```

页面状态：

```text
待机页 idle
展示内容页 page
```

### 状态一：待机页

触发：

```text
打开 /display 后默认显示。
```

内容来源：

```text
当前部署项目
```

显示内容：

```text
待机页小标题
待机页主标题
待机页提示
项目默认图片作为背景
项目主题色参与视觉
```

设计目标：

```text
远距离可读。
适合大屏长时间停留。
提示观众扫码。
不要出现复杂后台信息。
```

### 状态二：展示内容页

触发：

```text
收到扫码事件后立即显示。
也可以直接访问 /display?code=编号 进入。
```

内容来源：

```text
当前部署项目下的对应编号页面
```

显示内容：

```text
展项编号
展示页标题
展示页副标题
展示页正文
展示图片
主题色
```

图片规则：

```text
如果编号页面有展示图片，使用编号图片。
如果编号页面没有展示图片，使用项目默认图片。
```

主题色规则：

```text
优先使用编号页面主题色。
如果编号没有单独主题色，使用项目主题色。
```

设计目标：

```text
左侧大图，右侧文字信息。
标题要适合大屏远距离阅读。
正文不宜过长，建议用于 3 到 6 行核心介绍。
```

## 关键交互说明

### 项目切换

```text
后台选择项目，只影响当前编辑对象。
不会立即影响大屏。
```

### 项目部署

```text
点击“部署项目”后，该项目成为当前大屏和扫码使用的项目。
同一时间只允许一个项目处于部署状态。
```

### 编号保存

```text
保存编号只保存当前项目下的这个编号。
不会影响其它项目中同名编号。
```

### 模拟扫码

```text
后台点击“模拟扫码”会用当前编号触发 /api/scan。
只有当前项目已部署时才建议模拟扫码。
```

### 真机扫码

```text
C# Agent POST 到 /api/scan。
后端解析编号。
后端在当前部署项目下查找该编号。
如果不存在，会在当前部署项目下自动创建一个默认展示页。
```

## 前端设计建议

后台页面：

```text
以效率和清晰为主。
左侧固定为项目与编号导航。
右侧分为“项目级配置”和“编号页面配置”两个模块。
部署按钮需要明显，但不要和保存按钮混淆。
```

大屏页面：

```text
以远距离阅读为主。
少用小字。
主标题、编号、图片要有强视觉层级。
待机页、欢迎页、展示页之间的过渡要平滑。
视觉参考国际科技展会、贸易展主舞台和高端发布会大屏。
默认背景应有展馆空间感、LED 屏、灯光和纵深，避免普通网页卡片感。
展示页采用沉浸主图 + 信息面板结构，确保图片和文字都能被快速识别。
```

移动端：

```text
后台可以适配平板和笔记本。
大屏页主要按横向大屏设计，移动端只保证可查看。
```

## 当前接口摘要

公开接口：

```text
GET  /api/health
GET  /api/display/project
GET  /api/display/events
GET  /api/pages/{code}
POST /api/scan
POST /api/scans
```

后台接口：

```text
POST /api/login
POST /api/logout
GET  /api/session
GET  /api/projects
POST /api/projects
GET  /api/projects/{id}
PUT  /api/projects/{id}
POST /api/projects/{id}/deploy
GET  /api/projects/{id}/pages
PUT  /api/projects/{id}/pages/{code}
POST /api/assets
```

## 数据模型摘要

```text
projects
项目名称、待机页文案、欢迎页文案、默认图片、主题色、部署状态

pages
project_id、编号、展示页标题、副标题、正文、图片、主题色

scans
扫码编号、原始 URL、扫码时间

admin_users / admin_sessions
后台登录账号和会话
```
