# 展会现场启动说明

## 正式启动

双击：

```text
启动展会系统.bat
```

它会自动完成：

```text
1. 启动 C# 扫码 Agent
2. 启动 Python 展示后端
3. 自动打开大屏展示页
4. 运行期间定时检查扫码 Agent，关闭后会自动重新拉起
```

启动后不要关闭黑色命令行窗口。展会结束时再关闭它。

## 页面地址

```text
大屏展示页：http://127.0.0.1:8000/display
后台管理页：http://127.0.0.1:8000/admin
```

后台登录：

```text
账号：admin
密码：123456
```

如果浏览器没有自动打开，手动访问大屏展示页即可。

## 不要用于正式现场的启动方式

```powershell
python .\server.py
```

这个命令只启动 Python 后端，不会启动扫码 Agent。真实扫码器扫到内容后不会上报，所以看起来就是“扫码没反应”。

## 调试命令

只启动后端，不启动扫码 Agent：

```powershell
$env:START_SCANNER_AGENT='0'
python .\start_expo.py
```

启动时同时打开后台管理页：

```powershell
$env:OPEN_ADMIN='1'
python .\start_expo.py
```

不自动打开浏览器：

```powershell
$env:OPEN_BROWSER='0'
python .\start_expo.py
```

## 现场检查

正常运行时，任务管理器里应该能看到：

```text
python.exe
Txq_csharp_sdk.exe
```

如果只有 `python.exe`，说明扫码 Agent 没有运行，真实扫码不会触发展示页。
