# 开发记录

## 当前规范

展会扫码展示系统已经统一为项目内自包含部署。现场启动、打包和硬件调用都以当前 `expo-display` 目录为准。

## 启动入口

正式启动只使用：

```text
启动展会系统.bat
```

该脚本执行：

```text
start_expo.py
```

`start_expo.py` 负责：

```text
1. 启动项目内 C# 扫码 Agent
2. 启动 Python 展示后端
3. 自动打开大屏展示页
4. 定时看护 C# 扫码 Agent
```

## 扫码 Agent 规范

运行时只使用项目内文件：

```text
scanner-agent\Txq_csharp_sdk.exe
scanner-agent\Txq_csharp_sdk.exe.config
scanner-agent\vbar.dll
```

`start_expo.py` 不再查找项目外目录。判断 Agent 是否运行时，只识别当前项目内的 `scanner-agent\Txq_csharp_sdk.exe`。

启动项目前，如果系统里存在非当前项目目录下的同名 `Txq_csharp_sdk.exe`，启动器会先结束这些进程，避免扫码器被其它目录的 Agent 占用。

## 调用链路

```text
扫码器硬件
-> scanner-agent\vbar.dll
-> scanner-agent\Txq_csharp_sdk.exe --agent
-> POST http://127.0.0.1:8000/api/scan
-> server.py
-> GET /api/display/events
-> /display 大屏展示页更新
```

Python 不直接调用扫码器硬件。硬件访问由 C# Agent 完成，Python 只负责启动、看护和接收扫码结果。

扫码时只读取当前部署项目。每个项目维护自己的一套编号，同一个编号可以在不同项目下对应不同展示内容。

## Python 接口

```text
GET  /api/health
GET  /api/pages
GET  /api/pages/{code}
GET  /api/projects
POST /api/projects
GET  /api/projects/{id}
PUT  /api/projects/{id}
POST /api/projects/{id}/deploy
GET  /api/projects/{id}/pages
PUT  /api/projects/{id}/pages/{code}
PUT  /api/pages/{code}
POST /api/login
POST /api/logout
GET  /api/session
GET  /api/display/project
POST /api/scan
POST /api/scans
POST /api/assets
GET  /api/display/events
```

模拟扫码：

```powershell
Invoke-RestMethod -Method Post `
  -Uri 'http://127.0.0.1:8000/api/scan' `
  -ContentType 'application/json' `
  -Body '{"url":"https://achievementexpo.siat.ac.cn/view/#/activePage/10100043"}'
```

## 页面

```text
http://127.0.0.1:8000/display
http://127.0.0.1:8000/admin
```

后台当前支持新建展会项目、编辑项目待机页和欢迎页、编辑项目默认图片、为每个项目维护独立编号、部署项目、编辑展示页内容、上传图片、修改主题色和模拟扫码。

后台登录账号：

```text
admin / 123456
```

登录状态保存在 `admin_sessions` 表和浏览器 Cookie 中。`/admin`、内容保存接口和图片上传接口需要登录；扫码上报接口不需要登录，避免影响 C# Agent。

## 数据库

使用 SQLite：

```text
expo.db
```

当前表：

```text
admin_users
admin_sessions
projects
pages
scans
```

## 打包

打包使用：

```text
打包展会系统.bat
```

输出：

```text
expo-display-deploy.zip
```

部署时保留整个项目目录，至少包含：

```text
启动展会系统.bat
start_expo.py
server.py
expo.db
static
uploads
scanner-agent
```

## 已完成

```text
一键启动脚本
项目内 scanner-agent 自包含
C# Agent --agent 模式
Python 看护 C# Agent
扫码 POST 接口
SSE 大屏推送
后台内容编辑
后台登录
项目维度内容管理
项目部署切换
模拟扫码
打包脚本
部署说明
硬件接口说明
```

## 后续可选

```text
扫码 URL 域名白名单
开机自启动
现场屏幕比例专项优化
```
