# 打包部署说明

## 推荐顺序

正式交付前建议按这个顺序操作：

```text
1. 双击 运行门户质量检查.bat
2. 看到 portal_quality_gate_ok=true
3. 双击 打包展会系统.bat
4. 交付上一级目录生成的 expo-display-deploy.zip
```

不要在运行质量检查的同时打包。质量检查会读取 `expo-display-deploy.zip`，如果打包正在重写 zip，可能读到半写入文件。

## 质量检查

双击：

```text
运行门户质量检查.bat
```

它会检查前后台语法、蓝图数据、CSP/缓存规则、门户 API、二维码边界、系部页布局、后台路径、后台维护闭环和部署包完整性。

通过时会看到：

```text
portal_quality_gate_ok=true
```

## 打包方式

双击：

```text
打包展会系统.bat
```

它会在上一级目录生成：

```text
expo-display-deploy.zip
```

打包完成后，脚本会自动运行部署包完整性检查，确认 zip 内包含现场启动、质量检查、门户脚本、素材和文档，并排除备份、缓存、调试输出。

也可以在 PowerShell 中运行：

```powershell
python .\package_expo.py
```

## 打包范围

不要只打包 Python 文件，也不要只打包 `static`。现场运行需要这些内容保持在同一个文件夹内：

```text
启动展会系统.bat
运行门户质量检查.bat
打包展会系统.bat
start_expo.py
server.py
expo.db
static
uploads
scanner-agent
scripts
sucai
docs
```

打包脚本会排除本地备份、缓存和调试输出，例如 `__pycache__`、`*.backup-*`、`data.backup-*`、`blueprint.backup-*`。

## 扫码 Agent

扫码 Agent 文件在项目内：

```text
scanner-agent\Txq_csharp_sdk.exe
scanner-agent\Txq_csharp_sdk.exe.config
scanner-agent\vbar.dll
```

`start_expo.py` 会使用项目内的 `scanner-agent` 副本。部署和运行时都以这个目录为准。

## 现场启动

解压部署包后，双击：

```text
启动展会系统.bat
```

启动后保持命令行窗口打开。展会结束时再关闭它。

## 目标电脑要求

```text
Windows
Python 3
.NET Framework 4.8
扫码器驱动和 USB 连接正常
```

如果启动后扫码没反应，先检查任务管理器里是否同时存在：

```text
python.exe
Txq_csharp_sdk.exe
```
