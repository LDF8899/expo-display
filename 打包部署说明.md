# 打包部署说明

## 打包范围

直接打包整个 `expo-display` 文件夹。

也可以双击：

```text
打包展会系统.bat
```

它会在上一级目录生成：

```text
expo-display-deploy.zip
```

不要只打包 Python 文件，也不要只打包 `static`。现场运行需要以下内容都在同一个文件夹里：

```text
启动展会系统.bat
start_expo.py
server.py
expo.db
static
uploads
scanner-agent
```

## 扫码 Agent

`Txq_csharp_sdk.exe` 已经放到项目内：

```text
scanner-agent\Txq_csharp_sdk.exe
scanner-agent\Txq_csharp_sdk.exe.config
scanner-agent\vbar.dll
```

`start_expo.py` 只会使用这个项目内副本。部署和运行时都以 `scanner-agent` 目录为准。

## 现场启动

双击：

```text
启动展会系统.bat
```

启动后保持命令行窗口打开。展会结束时再关闭。

## 目标电脑要求

```text
Windows
Python 3
.NET Framework 4.8
扫码器驱动/USB 连接正常
```

如果启动后扫码没反应，先检查任务管理器里是否同时存在：

```text
python.exe
Txq_csharp_sdk.exe
```
