# 硬件调用接口说明

本文档说明展会扫码展示系统中“扫码器硬件”相关的调用链、运行时文件、接口约定和迁移注意事项。

项目单独移出时，重点保证 `scanner-agent` 目录完整。

## 总体链路

```text
扫码器硬件
-> vbar.dll
-> Txq_csharp_sdk.exe
-> POST http://127.0.0.1:8000/api/scan
-> Python server.py
-> SSE 推送给 /display
-> 大屏展示页面更新
```

Python 不直接调用扫码器硬件。硬件由 C# Agent 调用，Python 只负责启动、看护 C# Agent，并接收扫码结果。

## 运行时文件

项目内已经包含扫码 Agent：

```text
scanner-agent\Txq_csharp_sdk.exe
scanner-agent\Txq_csharp_sdk.exe.config
scanner-agent\vbar.dll
```

打包部署时必须保留整个 `scanner-agent` 目录。`Txq_csharp_sdk.exe` 和 `vbar.dll` 必须在同一个目录，否则 C# 程序无法加载硬件 DLL。

当前使用的是 64 位 DLL：

```text
scanner-agent\vbar.dll
```

对应 C# 程序也必须是 x64 构建。

## 启动方式

现场正式启动：

```text
启动展会系统.bat
```

该脚本会执行：

```text
start_expo.py
```

`start_expo.py` 只查找项目内 Agent：

```text
scanner-agent\Txq_csharp_sdk.exe
```

如果该文件不存在，扫码 Agent 不会启动。部署时必须保证 `scanner-agent` 目录完整。

## Python 与 C# Agent 的关系

`start_expo.py` 做三件事：

```text
1. 启动 C# 扫码 Agent
2. 启动 Python 展示后端
3. 定时检查 C# Agent 是否还在运行
```

启动 C# Agent 时使用参数：

```text
--agent
```

该参数表示隐藏原 WinForms 窗口，自动打开设备、自动打开扫码，不需要人工点击 SDK 示例窗口里的按钮。

运行期间如果 `Txq_csharp_sdk.exe` 被关闭，`start_expo.py` 会重新拉起。

启动项目前，如果系统里存在非当前项目目录下的同名 `Txq_csharp_sdk.exe`，`start_expo.py` 会先结束这些进程，避免扫码器被其它目录的 Agent 占用。

## C# Agent 硬件接口

硬件 DLL：

```text
vbar.dll
```

C# 封装文件：

```text
Vbarapi.cs
```

C# 通过 P/Invoke 调用 DLL 导出的 C 接口：

```csharp
vbar_channel_open(int type, long parm)
vbar_channel_send(IntPtr dev, byte[] data, int length)
vbar_channel_recv(IntPtr dev, byte[] buffer, int size, int milliseconds)
vbar_channel_close(IntPtr dev)
```

### 打开设备

C# 方法：

```csharp
openDevice()
```

内部调用：

```csharp
vbar_channel_open(1, 1)
```

返回非空句柄表示扫码器连接成功。

### 关闭设备

C# 方法：

```csharp
closeDevice()
```

内部调用：

```csharp
vbar_channel_close(dev)
```

### 打开或关闭扫码

C# 方法：

```csharp
controlScan(true)
controlScan(false)
```

打开扫码时发送 64 字节命令，核心命令字节：

```text
55 AA 05 01 00 00 FB
```

关闭扫码时发送：

```text
55 AA 05 01 00 01 FA
```

### 背光控制

C# 方法：

```csharp
backlight(true)
backlight(false)
```

当前展会 Agent 模式主要依赖自动打开设备和扫码，背光接口保留在 SDK 示例里。

### 读取扫码结果

C# 方法：

```csharp
getResultStr(out byte[] result_buffer, out int result_size)
```

内部调用：

```csharp
vbar_channel_recv(dev, buffer, 1024, 200)
```

返回数据包头判断条件：

```text
buffer[0] == 0x55
buffer[1] == 0xAA
buffer[3] == 0x00
```

数据长度：

```text
datalen = buffer[4] + (buffer[5] << 8)
```

扫码内容从数据包中取出后，C# 使用 UTF-8 解码为字符串。

## C# Agent 上报接口

C# Agent 得到扫码字符串后，会 POST 给 Python：

```text
POST http://127.0.0.1:8000/api/scan
Content-Type: application/json
```

请求体：

```json
{
  "url": "https://achievementexpo.siat.ac.cn/view/#/activePage/10100043"
}
```

Python 返回示例。线上二维码会原样跳转线上地址：

```json
{
  "ok": true,
  "scan": {
    "displayUrl": "https://achievementexpo.siat.ac.cn/view/#/activePage/10100043",
    "localDisplayUrl": "",
    "code": "10100043",
    "external": true
  },
  "clients": 1
}
```

后台生成的二维码会跳转本地配置好的展示页：

```json
{
  "ok": true,
  "scan": {
    "displayUrl": "/display?project=1&code=DEMO-10100043",
    "localDisplayUrl": "/display?project=1&code=DEMO-10100043",
    "code": "DEMO-10100043",
    "external": false
  },
  "clients": 1
}
```

如果 `clients` 为 `0`，说明当前没有打开的大屏展示页。C# Agent 会尝试打开本地展示页。

## Python 后端相关接口

健康检查：

```text
GET http://127.0.0.1:8000/api/health
```

扫码上报：

```text
POST http://127.0.0.1:8000/api/scan
POST http://127.0.0.1:8000/api/scans
```

大屏事件流：

```text
GET http://127.0.0.1:8000/api/display/events
```

展示页：

```text
http://127.0.0.1:8000/display
```

后台：

```text
http://127.0.0.1:8000/admin
```

## 环境变量

禁用 C# Agent 自动启动，只启动 Python：

```powershell
$env:START_SCANNER_AGENT='0'
python .\start_expo.py
```

不自动打开浏览器：

```powershell
$env:OPEN_BROWSER='0'
python .\start_expo.py
```

启动时同时打开后台：

```powershell
$env:OPEN_ADMIN='1'
python .\start_expo.py
```

调整 Agent 看护检查间隔，单位秒：

```powershell
$env:SCANNER_WATCH_INTERVAL='8'
python .\start_expo.py
```

## 单独迁移注意事项

打包整个 `expo-display` 文件夹即可。运行时只使用项目内 `scanner-agent` 目录。

必须保留：

```text
启动展会系统.bat
start_expo.py
server.py
expo.db
static
uploads
scanner-agent
```

目标电脑需要：

```text
Windows
Python 3
.NET Framework 4.8
扫码器 USB 连接正常
```

如需修改硬件调用逻辑，需要修改 C# 源码并重新构建 x64 版本，然后把新的构建产物覆盖到：

```text
scanner-agent\Txq_csharp_sdk.exe
scanner-agent\Txq_csharp_sdk.exe.config
scanner-agent\vbar.dll
```

## 常见问题排查

### 扫码没反应

先检查任务管理器是否同时存在：

```text
python.exe
Txq_csharp_sdk.exe
```

如果只有 `python.exe`，说明 C# Agent 没有运行。请使用：

```text
启动展会系统.bat
```

不要只运行：

```powershell
python .\server.py
```

`server.py` 只启动 Python 后端，不会启动硬件 Agent。

### Agent 启动失败

检查：

```text
scanner-agent\Txq_csharp_sdk.exe
scanner-agent\vbar.dll
```

两者必须在同一目录。`vbar.dll` 必须是 64 位版本。

### 后端正常但大屏不跳转

检查大屏页是否已打开：

```text
http://127.0.0.1:8000/display
```

再访问健康接口：

```text
http://127.0.0.1:8000/api/health
```

其中 `clients` 大于 `0` 表示已有展示页连接事件流。

### Python 接口测试

可以不用硬件，直接模拟扫码：

```powershell
Invoke-RestMethod -Method Post `
  -Uri 'http://127.0.0.1:8000/api/scan' `
  -ContentType 'application/json' `
  -Body '{"url":"https://achievementexpo.siat.ac.cn/view/#/activePage/10100043"}'
```

如果模拟扫码能跳转，但真实扫码不跳转，问题在扫码器、`vbar.dll`、C# Agent 或 USB 连接层。
